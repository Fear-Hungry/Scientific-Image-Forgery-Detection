# weights_cache (bundle)

Esta pasta existe para facilitar o uso offline no Kaggle quando você empacota o repositório como Dataset.

- Coloque aqui os arquivos `<model_name>.pth` (ex.: `tf_efficientnet_b4_ns.pth`).
- O notebook procura também em `<dataset>/recodai_bundle/weights_cache/`.
